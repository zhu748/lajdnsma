# 更新日志
* v1.0.13（第七轮优化：key 亲和重试——只有配额耗尽才换 key）
   * **策略重构：换 key 条件收紧为"配额耗尽"**——用户明确要求"只有出现配额耗尽的时候才换 key，其他情况一律用原 key 继续重试"。旧逻辑 429/401/403/500/503/网络错误全部触发冷却并轮换；新逻辑只有 429（分钟级/日级配额耗尽，冷却时长采纳上游 retryDelay）与 401/403（密钥已失效，重试永远不可能成功）才让出 key
   * **500/503 不再冷却换 key**——上游内部错误/过载与 key 本身无关，换 key 对成功率毫无帮助，反而把"多 key 池轮换"指纹暴露给上游风控；现在这两个错误原 key 带 full-jitter 退避重试
   * **网络类错误（超时/连接失败/DNS/TLS）不再冷却换 key**——连接层故障同样与 key 无关（换 key 救不了网络黑洞），原 key 退避重试；此项调整了第六轮引入的 30s 短冷却策略
   * **新增 key 亲和机制（preferred_keys）**——`select_valid_api_keys` 新增 preferred_keys 参数：上一批因非配额耗尽（500/503/网络错误/空响应）失败的 key，下一批优先重用（经冷却/RPM/日额度三重校验）；polling 轮换模式下这也是同 key 重试的唯一保证（否则每次选 key 都会 pop 到下一个）
   * **新增失败分类器（quota/dead/transient）**——`handle_gemini_error` 入口**同步**记录每个 key 最近一次失败的类别（不依赖 fire-and-forget 冷却任务的调度时序），重试循环据此决定下一批是换 key 还是继续用原 key；429→quota（换）、401/403→dead（换）、500/503/网络/未知→transient（不换）
   * **修复：亲和与轮换的环检测冲突**——亲和阶段被跳过的 key（如期间被并发请求打成 429）若混入轮换环检测集，正常轮换循环一碰到它就误判"已试完全部 key"提前 break，池内其余健康 key 全部被无视；现亲和去重集与轮换环检测集分离
   * 可观测性：key 亲和重用时记录"key affinity retry: reusing key#xxx (transient failure, quota not exhausted)"日志，运维可直接验证"非配额错误不换 key"是否生效
   * 测试：新增 30 项第七轮回归测试（失败分类/同步记录/冷却门控/亲和选择/重试循环模式），全套 255 项测试通过
* v1.0.12（第六轮优化：防风控闭环 + 详细错误日志 + 性能 + 前端体验）
   * **修复：网络类错误不冷却密钥**——超时/连接失败/DNS 故障无 HTTP 状态码，旧冷却逻辑直接跳过；默认 fill（粘性）模式下同一个"黑洞 key"会烧光全部 15 次重试。现传输层错误触发 30s 短冷却，重试循环立即切换健康 key
   * **修复：fill 模式 RPM 满导致整批失败**——粘滞 key 达 RPM 退避阈值被跳过后，下一轮仍拿到同一 key，去重即 break，返回空批次（健康 key 完全没被尝试，客户端直接收到失败）。现 RPM 跳过时让出粘滞位（`advance_sticky_key`），本轮继续评估后续 key
   * **修复：429 冷却不看上游指示**——现解析 429 响应体的 `retryDelay`（限制在 5s~1h），分钟级限流不再白等 55s，日配额耗尽不再每分钟被拉起来反复撞限速；日志同时记录配额维度（PerMinute/PerDay）
   * **修复：上游错误体被静默丢弃**——非 200 响应体（429 配额详情、5xx 具体原因、403 拒绝理由）现在经脱敏后完整记入面板日志，排障不再只有状态码可看
   * **修复：未捕获异常丢失堆栈**——全局异常钩子现在附带末尾堆栈帧；asyncio 事件循环异常处理器接管孤儿 Task/回调异常并路由到面板日志（此前只打 stderr，网页上看不到）
   * **修复：密钥探测失败原因不可区分**——网络故障与密钥无效现在分别记录（HTTP 401/403 = 密钥无效；网络异常 = 连不上上游），排障方向不再被带偏
   * **修复：缓存串答**——缓存键此前只哈希模型+工具+消息，temperature/max_tokens/top_p 等采样参数不同也共享缓存；现已纳入生成参数（Gemini 原生格式含 thinkingConfig/topK 等）
   * 优化：**HTTP/2 出站支持**——官方 SDK 与 Chrome 的 ALPN 均协商 h2，此前只报 http/1.1 的 TLS 指纹与伪装 UA 自相矛盾；新增 `h2` 依赖，未安装时优雅降级
   * 优化：**浏览器 UA 补齐配套头**——UA 选中 Chrome 时补充 `sec-ch-ua`/`sec-fetch-*`/`Origin`/`Referer`（版本与 UA 联动），消除"Chrome UA 无 sec 头"这一头部层指纹
   * 优化：**分层超时**替代 600s 标量——connect=10s（黑洞连接快速失败）/read=300s（兼容 thinking 长响应）/write=60s/pool=10s，卡死连接不再占用 key 槽位 10 分钟；僵尸任务清理阈值 300s→620s 与超时对齐，合法长请求不再被误杀成模糊 500
   * 优化：**RPM 窗口发射时计数**——旧逻辑只在请求成功完成时计数，在途/失败/超时请求全部漏记（并发越高低估越严重）；现在三个发射点同时记录，退避不再系统性迟到
   * 优化：**缓存容量驱逐改堆式精确 LRU**——旧实现全量收集+nsmallest+逐项日志且全程持锁（O(N²) 日志刷屏）；现在均摊 O(logN)、日志合并单行
   * 优化：限流提前到缓存键计算之前（大 body 高频请求不再强制服务端做重复重哈希）
   * 前端：KPI 卡片新增图标徽标；新增数据新鲜度指示条（"刚刚更新/N 秒前"）；日志面板新增复制/下载按钮与智能滚动跟随（翻历史自动暂停、回底部自动恢复）；密钥表格新增日额度用量进度条（按水位变色）；图表数据刷新平滑动画；刷新按钮加载旋转反馈
   * 测试：新增 32 项第六轮回归测试（网络错误冷却/retryDelay 解析/fill 让位/发射计数/缓存参数键/LRU 驱逐/浏览器指纹头/分层超时），全套 225 项测试通过
* v1.0.11（第五轮优化：反风控 RPM 窗口 + 可观测性 + 逻辑修复）
   * **修复：per-key RPM 限流窗口在高负载下失效**——旧实现依赖全局最近 100 条调用记录统计每 key 分钟调用量，总调用量超过 100 次/分钟后窗口记录先被全局淘汰，per-key 计数恒为 0，密钥选择层的 RPM 退避完全失效（负载越高越失效），热 key 持续被打穿上游 429；现改为每 key 独立的 60s 时间戳滑动窗口（每 key 上限 120 条、写入/读取时自动裁剪过期项）
   * **修复：仪表盘日志面板被刷屏淹没**——每笔 API 调用都写一条"API调用已记录"日志，中等 QPS 下几秒内就把冷却触发、429、密钥失败等真正需要运维关注的告警全部挤出有界日志缓冲；现已移除该逐笔日志（每请求仍有带 key/model 上下文的起止日志）
   * 优化：日志缓冲容量 100 → 200 条，仪表盘可回看更多历史日志
   * **修复：密钥检测后陈旧冷却状态不清理**——曾被 401/403 永久拉黑的密钥，即使重新检测判定有效，运行时也会因陈旧的永久拉黑条目被永远跳过（面板显示"有效"但从不使用）；现检测完成后自动清空全部冷却状态
   * 文档：密钥选择层补充 fill（粘性）模式每批恒为 1 个 key 的语义说明（有意的反指纹设计，非 bug）
* v1.0.10（第四轮优化：反风控 + UI 修复 + 性能）
   * **修复：密钥冷却机制失效**——冷却标记仅存在于从未被调用的错误处理函数中，429 限流密钥会反复被选中反复撞限速；现已激活，429 冷却 60s、500/503 冷却 5s、401/403 永久拉黑
   * **修复：重试风暴**——聊天重试批次失败后原先零间隔立即发射下一批（最多 15 个 key 连环）；现批间加入 full-jitter 指数退避（0.5s 起、8s 封顶），成功路径零延迟
   * **修复：仪表盘图表恒为零**——前端读取字段与后端返回字段不一致（count vs value），图表无论真实调用量多少都画零平线
   * 修复：`x-goog-api-client` 头与随机 User-Agent 联动（此前 UA 选到 Chrome/curl 却携带 SDK 头，自相矛盾易被识别）
   * 修复：新版 `AQ.` 前缀密钥在上游错误串中未脱敏的问题
   * 优化：UA 指纹池扩充至 13 条并更新到当前主流版本段（Chrome 13x / genai-sdk 1.x，保留旧版本模拟自然分布）
   * 优化：启动密钥探测超时 60s→15s，探测间隔加 0.3-0.8s jitter（消除密钥枚举节奏特征，加速最坏情况启动 4 倍）
   * 优化：前端后台标签页自动暂停仪表盘轮询，回到前台立即刷新；移除日志/图表组件冗余 deep watch
* v0.3.5
   * 修改vertex模式拉取模型链接，为新模型做准备
* v0.3.4
   * 修复持久化问题
* 历史版本更新日志请查看[update](./update.md)
* v0.3.0
   * 翻新vertex模式相关功能，规范拉取模型列表
   * 修复若干bug，提高系统稳定性
* v0.2.8
   * 修复vertex模式下0325exp无法使用的问题
   
* v0.2.7
   * 支持前端配置热更新vertex json
   * 支持前端配置vertex快速模式key
   
* v0.2.6
   * 为ai studio新增gemini原生接口支持，接口格式为/gemini
   * 优化了负载均衡策略
   * 修改了前端样式与配置逻辑，为前端配置选项新增`空响应重试次数`配置
   * 新增windows一键安装脚本
   * 解决 500/503 失败不重试问题
   * 修复若干bug，提高系统稳定性
   
* v0.2.5
   * 警告，本次新增了openai依赖库，非容器部署请拉取安装依赖库
   * 首发适配vertex快速模式，在配置key后，访问支持快速模式的模型时，将首先使用快速模式的key访问
   * 快速模式仅支持gemini-2.0-flash-001,gemini-2.0-flash-lite-001,gemini-2.5-pro-preview-03-25,gemini-2.5-flash-preview-04-17
   * 更新vertex模式ui，前端界面现在可以开启关闭假流式，快速模式与替换快速模式key
   * 新增环境变量`VERTEX_EXPRESS_API_KEY`，为快速模式key
   * 修复若干bug，提高系统稳定性

* v0.2.4
   * 为vertex模式适配gemini-2.5-pro-preview-05-06
   * 修复负载均衡模式bug
   * 优化统计相关计算，占用性能更少
   * 修复若干bug，提高系统稳定性

* v0.2.3
   * 优化了负载均衡策略
   * 重构了持久化模块
   * 新增了模型白名单和请求头白名单模式，对应环境变量`WHITELIST_MODELS`和`WHITELIST_USER_AGENT`
   * 修复了夜间模式下文字颜色显示不正确的问题
   * 前端新增了对 API Key 可用性的检查功能
   * 修复了若干已知问题
* v0.2.2
    * 实现函数调用支持，包括流式和非流式函数调用
    * 修复了dockerfile不使用uv的问题
    * 修复了SKIP_CHECK_API_KEY环境变量相关问题
    * 采用最后8条消息计算缓存键，提升缓存效率
    * 新增key后，如果之前的模型列表为空，会自动拉取模型列表，因此现在不需要在环境变量中预先添加key，可以直接在前端界面添加使用
* v0.2.1beta
    * 为vps/本地部署用户提供持久化支持（测试版），现在所有的配置选项与请求记录都可以持久化存储
    * 为前端添加API实时调用统计图表
    * 为前端新增添加API秘钥功能（注意，如果您没有配置持久化，在这里添加的key在项目重启后会丢失）
    * 添加对函数调用的支持
    * 新增环境变量`STORAGE_DIR`，为持久化存储目录，默认为`/hajimi/settings/`
    * 新增环境变量`ENABLE_STORAGE`，为是否启用持久化，默认为`false` 
    * 修复若干bug，提高系统稳定性
* v0.2.0
    * ui全面焕新，更动态更好看，为api key显示添加分页，适配拥有大量key的用户
    * 隔离vertex模式与ai studio模式日志，ai studio玩家再也不会看到vertex的报错了
    * 拆分web ui密码与连接密码，现在可以安全的把反代分享给其他人了
    * 新增环境变量`WEB_PASSWORD`，为web ui修改设置密码，默认值为环境变量`PASSWORD`的值
    * 修复若干bug，增加系统稳定性
* v0.1.7
    * 添加token计数器，现在可以在前端看到每一个api使用的token数与单模型使用的token数量了
    * 为2.5flash模型适配`thinkingBudget`参数，用户只需在附加参数中添加`- thinking_budget : 1024`即可调整
    * 修复在报错中错误漏出key的问题
    * 为并发缓存功能实现缓存键值计算切换，用户可选择使用最后八条消息或全部消息计算缓存
    * 新增环境变量`PRECISE_CACHE`，为切换缓存计算方法，默认为`false`
    * 修复若干bug
* v0.1.6
    * 为并发模式提供缓存，并发中除了返回的成功请求，其他成功请求将被缓存，若下次请求与本次请求一致，将首先使用缓存内容
    * 为请求添加硬限制，超过每日限定次数的key将不再发送请求，若所有key都达到次数限制，将随机选择一个key请求
    * 添加空响应计数，单次请求空响应重试超过一定次数将直接跳出
    * 修复联网模式bug（感谢**yoolieer**），现在能够正常的启用停用联网模式了
    * 新增环境变量`MAX_EMPTY_RESPONSES`，为空响应重试次数，默认为5
    * 修复若干bug
* v0.1.5
    * 实现vertex热切换，现在在前端面板就可以切换vertex模式与ai studio模式了
    * 为vertex模式实现假流式，环境变量与ai studio模式的假流式相同，均为`FAKE_STREAMING`
    * 优化前端界面
    * 修复若干bug
* v0.1.4beta
    * 为大部分配置项适配热更新，可实时调整配置
    * 前端界面新增适配热更新相关ui界面，可直接调整配置（在前端界面修改的数据在重启后会失效）
    * 适配 vertex ai（基于gzzhongqi/vertex2openai项目开发），在启用vertex模式并配置vertex凭证后，项目将切换为vertex请求模式
    * 新增环境变量`ENABLE_VERTEX`用于启用vertex模式，初始默认为false
    * 新增环境变量`GOOGLE_CREDENTIALS_JSON`用于配置vertex凭证，默认为空
* v0.1.3
    * 应对谷歌加强封锁紧急更新，优化连接逻辑，减少错误发生
    * 修复了在非流式模式下日志暴露key的问题
    * 修复前端界面联网模式无法正确显示
    * 修复前端界面伪装模式字符数量无法正确显示
* v0.1.2beta
    * 为非流式和假流式传输模式新增动态并发功能，用户可自定义初始并发请求数。在全部请求失败时，系统将增加并发请求数，直至达到最大并发请求数。当收到请求时，程序会首先尝试并发处理，若并发处理失败，则根据设定逐步增加并发数。
    * 重构请求处理逻辑，现在能够正确处理各个模式的请求。
    * 新增手动重置统计数据按钮，用户可在点击按钮后输入password重置统计数据
    * 前端环境配置栏目将展示更多功能配置，同时展示卡可折叠
    * 修改重置统计数据时间为北京时间15点
    * 修复若干bug
    * 新增环境变量`CONCURRENT_REQUESTS`用于设置默认的并发请求数，初始默认值为1。
    * 新增环境变量`INCREASE_CONCURRENT_ON_FAILURE`用于设置当请求失败时增加的并发请求数，初始默认值为1。
    * 新增环境变量`MAX_CONCURRENT_REQUESTS`用于设置允许的最大并发请求数，初始默认值为3。
*   v0.1.1
    * 新增联网模式,为所有gemini2.x模型提供联网能力，在模型列表中选择-search后缀的模型启用
    * 新增环境变量`SEARCH_MODE`是否启用联网模式，默认为true
    * 新增环境变量`SEARCH_PROMPT`为联网模式提示词，默认为`（使用搜索工具联网搜索，需要在content中结合搜索内容）`
*   v0.1.0
    * 使用vue重写前端界面，适配移动端
    * 前端界面添加黑夜模式
    * 支持为多模态模型上传图片
    * 可用秘钥数量将异步更新，防止阻塞进程
    * 这次真能北京时间16点自动重置统计数据了
    * 为api秘钥使用统计新增模型使用统计，可分别统计使用不同模型的次数
    * 修改默认api可用次数为100次
    * 降低默认伪装信息长度为5，以减少对上下文的污染
*   v0.0.5beta
    * 新增"**伪装信息**功能，默认开启，可在转发消息中添加随机字符串伪装消息，防止被检测
    * 修复若干bug
    * 为前端界面新增**功能配置**栏目，可检查功能是否开启
    * 北京时间16点自动重置统计数据
    * 在环境变量中新增`RANDOM_STRING`，是否启用伪装信息，默认值为true
    * 在环境变量中新增`RANDOM_STRING_LENGTH`，伪装信息长度，默认为20
    * 为git用户提供单独的`Dockerfile_git`
*   v0.0.4
    * 修改版本更新逻辑，现在为每四小时检查一次版本更新
    * 前端界面所有数据数据实现动态更新
    * 新增**单api使用次数统计**，在原API调用统计下方新增可折叠的单api使用次数统计，同时提供进度条查看剩余使用次数
    * 在环境变量中新增`API_KEY_DAILY_LIMIT`，为单api 24小时最大使用次数，默认值为25
    * 在环境变量中新增`BLOCKED_MODELS`，为需要屏蔽的模型名称，多个模型用英文逗号分隔
*   v0.0.3beta
    * 完善了客户端断开连接的处理逻辑（感谢[@warming-afternoon](https://github.com/warming-afternoon)）
    * 新增"假流式传输模式"，该模式默认开启，以解决在某些情况下客户端断开连接的问题。如需关闭，请将环境变量 `FAKE_STREAMING` 设置为 `false`。
*   v0.0.2 修复了在log中错误暴露apikey的问题，修改了客户端断开连接的处理逻辑（感谢[@warming-afternoon](https://github.com/warming-afternoon)）
